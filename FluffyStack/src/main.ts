import { initLayout } from './components/layout.ts';

initLayout();

// if (document.readyState === 'loading') {
//     document.addEventListener('DOMContentLoaded', initLayout);
// } else {
//     initLayout();
// }